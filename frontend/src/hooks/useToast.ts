/**
 * useToast — Re-export of the toast hook from ToastContext for convenience.
 * Import from hooks/ to keep consumer code decoupled from context internals.
 * @module hooks/useToast
 */
export { useToast } from '../contexts/ToastContext';
